<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/init_nav"
    app:startDestination="@id/login2">

    <fragment
        android:id="@+id/login2"
        android:name="com.example.aimanmuneeb.ui.Login"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login" >
        <action
            android:id="@+id/action_login2_to_navActivity"
            app:destination="@id/navActivity" />
    </fragment>
    <activity
        android:id="@+id/navActivity"
        android:name="com.example.aimanmuneeb.NavActivity"
        android:label="activity_nav"
        tools:layout="@layout/activity_nav" />
</navigation>